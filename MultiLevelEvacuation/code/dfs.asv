function pixels=dfs(index,image)
    pixels=[index]
    image(index)=0
    for x=[index(1)-1,index(1)+1]
        for y=[index(2)-1,index(2)+1]
            if(image(x,y)=0)
                
            end
        end
    end
end